apiVersion: v1
kind: Service
metadata:
  namespace: supply
  creationTimestamp: null
  labels:
    app: admin-supp
    app.kubernetes.io/managed-by: Helm
  name: {{ .Values.adminBack.metadata.svcName }}
spec:
  ports:
  - port: {{ .Values.adminBack.service.ports.port }}
    protocol: TCP
    targetPort: {{ .Values.adminBack.service.ports.targetPort  }}
    nodePort: {{ .Values.adminBack.service.ports.NodePort }}
  selector:
    app: admin-supp
  type: {{ .Values.serviceType.NodePort}} 
status:
  loadBalancer: {}
